# Client CMS (igm-cms / Payload)
CMS_API_URL=http://localhost:3001/api
CMS_API_TOKEN=

# CDN public (CloudFront) — même valeur que NEXT_PUBLIC_CDN_URL côté cms
NEXT_PUBLIC_CDN_URL=

# Secret partagé avec igm-cms (webhook revalidation ISR)
REVALIDATE_SECRET=change-me-shared-secret
