/**
 * Contenu des pages Qui sommes-nous (/a-propos, /historique, /mission).
 * `true` = global Payload « Page À propos ». `false` = seed statique local (fallback dev).
 */
export const WHO_WE_ARE_USE_CMS = true;

/** Section « Nos partenaires » sur la page d'accueil. */
export const HOME_SHOW_PARTNERS_SECTION = false;
